
제로값: 선언되었지만 값이 할당되지 않은 변수에 대한 기본값 기본적으로 0이나 빈문자열을 말함 int = 0 , bool = false , string = ""

부동소수점 (float32/64)는 정확한 계산이 아니니 돈계산같이 정확한 계산을 요구하는곳에선 사용하지 말것

문자열: go에서 문자열은 불변 "재 할당"은 가능하지만 "이미 할당된 값"은 변경 불가

명시적 타입변환: go는 자동 타입변환을 허용하지 않으므로 명시적인 타입변환을 해줘야함

변수 선언 var vs :=  var 로는 제로값을 할당할수있음(var x int)  :=로는 이미 할당되있더라도 할당 안된것과 같이 재할당가능 (x,err(이미 할당된변수 err) := )
    주의: :=는 함수밖에서는 사용이 안되기때문에 패키지 단위로 사용할 변수는 var 키워드를 사용해야함
    var
        var x int = 10 ,  var x = 10  , var x int
    :=
        x := 10

    